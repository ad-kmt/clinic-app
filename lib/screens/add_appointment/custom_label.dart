import 'package:flutter/material.dart';

import '../../constants.dart';

Widget buildCustomLabel(String label) => Text(
      label,
      style: TextStyle(
        color: kTextLight2,
        fontSize: 12,
      ),
    );
